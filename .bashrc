#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
alias store='git --git-dir=/home/pi/.dots.d/ --work-tree=/home/pi'
